# قاعدة البيانات

التطبيق يستخدم MySQL عبر Drizzle ORM، وليس LocalStorage.

يجب ضبط `DATABASE_URL` في بيئة تشغيل الخادم، مثل:

`mysql://USER:PASSWORD@HOST:3306/DATABASE`

بعد تشغيل الخادم يجب تنفيذ ترحيلات Drizzle الموجودة في المشروع قبل استخدام العملاء والأصناف والفواتير.

إذا لم تكن `DATABASE_URL` موجودة أو كانت قاعدة البيانات غير قابلة للوصول، يجب أن تظهر رسالة خطأ واضحة بدل إعطاء المستخدم رسالة نجاح وهمية.
