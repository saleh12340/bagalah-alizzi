# خطة تصميم تطبيق بقالة العزي للمواد الغذائية

## التوجه العام

التطبيق مخصص للاستخدام اليومي السريع داخل متجر بقالة، لذلك تعتمد التجربة على واجهة عربية RTL، وشريط تبويب سفلي واضح، وأزرار كبيرة قابلة للمس بيد واحدة. الأولوية هي الوصول إلى إنشاء فاتورة خلال أقل عدد ممكن من الخطوات، مع إبقاء إجمالي السلة ظاهرًا دائمًا.

## الهوية البصرية

يستخدم التطبيق لونًا أخضر زمرديًا رئيسيًا `#087F5B` للدلالة على المنتجات والنمو، مع خلفية عاجية هادئة `#F7F8F5` وبطاقات بيضاء `#FFFFFF`. تستخدم ألوان الحالة الأخضر `#1F9D68` للسداد والنجاح، الأحمر `#D64545` للديون والتنبيهات، والأزرق `#2878C8` للفواتير والمعلومات. النص الأساسي فحمي `#17221D` والنص الثانوي `#718078`، مع حدود فاتحة `#E4EAE6`. يجب استخدام خط عربي واضح مثل Cairo أو Noto Arabic عند توفره.

## قائمة الشاشات

| الشاشة | المحتوى والوظائف |
|---|---|
| الرئيسية | ملخص مبيعات اليوم، إجمالي الديون، تنبيهات المخزون المنخفض، اختصارات إنشاء فاتورة وإضافة منتج، وآخر الفواتير. |
| الفواتير | قائمة الفواتير مع البحث والتصفية، وفتح تفاصيل فاتورة أو إعادة طباعتها أو مشاركتها. |
| إنشاء فاتورة | اختيار نقدي أو عميل، بحث المنتجات، إضافة الكمية، تعديل السعر والكمية، الخصم، طريقة الدفع، وحفظ الفاتورة. |
| تفاصيل الفاتورة | رقم الفاتورة والتاريخ والعميل والأصناف والإجمالي، مع إجراءات PDF والطباعة وواتساب. |
| العملاء | بطاقات العملاء بالاسم والهاتف والرصيد الحالي، مع إضافة عميل وفتح الحساب بالنقر على البطاقة. |
| حساب العميل | بيانات العميل، الرصيد، سجل الفواتير والدفعات وتعديل الرصيد، مع إضافة دفعة وتصدير كشف حساب. |
| المنتجات والمخزون | قائمة المنتجات، البحث والتصنيف، الكمية الحالية، تنبيه الحد الأدنى، وإضافة/تعديل المنتج مع منع التكرار. |
| التقارير | تقرير اليوم أو الأسبوع أو الشهر أو فترة مخصصة، مع مؤشرات المبيعات والأرباح والمصروفات والديون وحركة المخزون. |
| الموردون | قائمة الموردين وبيانات الهاتف والفواتير والديون والدفعات. |
| المصروفات والملاحظات | إضافة مصروف بسبب وقيمة وتاريخ، وإنشاء وتعديل وحذف ملاحظات المتجر. |
| الإعدادات | اسم المتجر وشعاره ورقم الهاتف والطابعة ومقاس الورق والنسخ الاحتياطي والاستعادة. |

## التنقل

يستخدم شريط تبويب سفلي من أربع وجهات: الرئيسية، الفواتير، العملاء، والمخزون. تظهر الإجراءات الأقل استخدامًا مثل التقارير والموردين والمصروفات والإعدادات من قائمة «المزيد» في الشاشة الرئيسية. زر الإجراء الرئيسي في الشاشة الرئيسية يفتح إنشاء فاتورة مباشرة.

## تدفقات الاستخدام الأساسية

### إنشاء فاتورة نقدية

1. يضغط المستخدم «فاتورة جديدة».
2. يترك نوع البيع «نقدي» أو يختار عميلًا.
3. يكتب اسم المنتج في البحث ويختار النتيجة.
4. تعدل الكمية من صف الصنف، ويحسب الإجمالي تلقائيًا.
5. يضيف المستخدم خصمًا اختياريًا ويختار طريقة الدفع.
6. يضغط «حفظ الفاتورة»، فيظهر تأكيد مع خيارات الطباعة وPDF والمشاركة.

### البيع الآجل على حساب عميل

1. يفتح المستخدم فاتورة جديدة ويختار عميلًا.
2. يضيف الأصناف ويختار «آجل».
3. يحفظ التطبيق الفاتورة ويزيد رصيد العميل ويخصم الكميات من المخزون.
4. يفتح المستخدم تفاصيل الفاتورة أو حساب العميل لإرسال كشف أو تسجيل دفعة.

### تسجيل دفعة عميل

1. يفتح المستخدم بطاقة العميل.
2. يضغط «إضافة دفعة».
3. يدخل المبلغ والملاحظة، ثم يحفظ العملية.
4. يتحدث الرصيد ويظهر سجل العملية باللون الأخضر.

### إدارة المخزون

1. يفتح المستخدم المخزون ويبحث عن المنتج.
2. يضغط «إضافة منتج» أو يفتح منتجًا موجودًا للتعديل.
3. يدخل سعر الشراء والبيع والكمية والحد الأدنى.
4. عند وجود اسم مكرر، يظهر تنبيه يتيح التحديث أو إنشاء نسخة اختيارية.

## قواعد واجهة مهمة

يجب ألا تكون هناك أزرار بلا نتيجة، ويجب إظهار حالة الحفظ بوضوح. تستخدم القوائم الطويلة FlatList، وتستخدم كل شاشة حاوية آمنة، مع دعم لوحة المفاتيح والتمرير. تبقى قيمة السلة والإجراء الأساسي قريبين من أسفل الشاشة لتسهيل الاستخدام بيد واحدة. وظائف الطباعة الحرارية وPDF وواتساب تُنفذ على مراحل بعد استقرار تدفق الفاتورة والبيانات.
